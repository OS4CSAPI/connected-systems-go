# Issues Evaluation Plan — `OS4CSAPI/connected-systems-go`

**Purpose.** Conduct a deep, thorough, comprehensive evaluation of every open issue
in our `connected-systems-go` fork to determine each issue's **validity,
legitimacy, accuracy, and completeness** before any of them are acted on,
escalated, or closed.

**Why this plan exists.** The issue tracker is the single source of truth that
will drive future development priority. Issues filed in haste, derived from
incomplete code reads, or based on misidentified failure modes will silently
distort that priority. Investing the time to audit each issue once — methodically
and with citations — pays back across every downstream decision.

**Scope.** All issues open at the start of this effort on
<https://github.com/OS4CSAPI/connected-systems-go/issues>. PRs are out of scope
for this plan. New issues filed after the start date are handled as a follow-up.

**Non-goals.** This plan does **not** implement fixes, propose patches, or
re-prioritize roadmap items. Those are downstream activities. The deliverable is
a per-issue evaluation record only.

---

## Working agreement

1. **One issue at a time.** No batch evaluations. Each issue gets a dedicated
   evaluation pass and a dedicated record.
2. **Cite, don't claim.** Every finding-of-fact in an evaluation must reference
   either (a) a specific file:line in the cs-go source tree, (b) an authoritative
   external standard (OGC API, WHATWG, RFC, IETF), or (c) a verbatim quote from
   the issue body. Unsourced assertions are not acceptable.
3. **Distinguish layers of confidence.** Use the rubric below. Do not collapse
   "the issue describes a real symptom" with "the issue's diagnosis is correct"
   with "the issue's proposed fix is appropriate." Those are three separate
   judgments.
4. **Admit uncertainty.** If an evaluation cannot reach a conclusion in a given
   dimension without further code execution, server access, or upstream
   clarification, the record says so explicitly. We do not invent confidence.
5. **No issue is closed or commented on GitHub** as part of this effort without
   explicit user approval per issue. The plan produces *recommendations*; acting
   on them is a separate decision.

---

## Authoritative resources

The sibling planning repo `OS4CSAPI/ogc-client-CSAPI_2` (branch `phase-7`) has
an extensive `docs/research/` tree of pre-digested CSAPI specification material
and authoritative references. **Spec-related verdicts in our evaluations must
cite one of these resources or the upstream standard directly.** "I think the
spec says…" is not acceptable.

### Primary (machine-readable, authoritative)

- **OGC API – Connected Systems Part 1, bundled OpenAPI 3.1** —
  [`docs/research/standards/ogcapi-connectedsystems-1.bundled.oas31.yaml`](https://github.com/OS4CSAPI/ogc-client-CSAPI_2/blob/phase-7/docs/research/standards/ogcapi-connectedsystems-1.bundled.oas31.yaml)
  · all `$ref`s resolved · use for endpoint paths, request/response schemas,
  query parameters, conformance classes for Systems / Deployments / Procedures /
  SamplingFeatures / Properties / Features.
- **OGC API – Connected Systems Part 2, bundled OpenAPI 3.1** —
  [`docs/research/standards/ogcapi-connectedsystems-2.bundled.oas31.yaml`](https://github.com/OS4CSAPI/ogc-client-CSAPI_2/blob/phase-7/docs/research/standards/ogcapi-connectedsystems-2.bundled.oas31.yaml)
  · all `$ref`s resolved · use for Datastreams / Observations / ControlStreams /
  Commands / SystemEvents / SystemHistory.

### Pre-digested spec extractions (use these to find the right OpenAPI region)

- [`docs/research/requirements/csapi-part1-requirements.md`](https://github.com/OS4CSAPI/ogc-client-CSAPI_2/blob/phase-7/docs/research/requirements/csapi-part1-requirements.md)
- [`docs/research/requirements/csapi-part2-requirements.md`](https://github.com/OS4CSAPI/ogc-client-CSAPI_2/blob/phase-7/docs/research/requirements/csapi-part2-requirements.md)
- [`docs/research/requirements/csapi-crud-operations.md`](https://github.com/OS4CSAPI/ogc-client-CSAPI_2/blob/phase-7/docs/research/requirements/csapi-crud-operations.md)
  — directly relevant to issues #1, #2, #7, #12.
- [`docs/research/requirements/csapi-query-parameters.md`](https://github.com/OS4CSAPI/ogc-client-CSAPI_2/blob/phase-7/docs/research/requirements/csapi-query-parameters.md)
  — directly relevant to issues #7, #8, #9, #11.
- [`docs/research/requirements/csapi-format-requirements.md`](https://github.com/OS4CSAPI/ogc-client-CSAPI_2/blob/phase-7/docs/research/requirements/csapi-format-requirements.md)
  — directly relevant to issues #3, #4, #5.
- [`docs/research/requirements/csapi-datatype-schema-requirements.md`](https://github.com/OS4CSAPI/ogc-client-CSAPI_2/blob/phase-7/docs/research/requirements/csapi-datatype-schema-requirements.md)
- [`docs/research/requirements/csapi-conformance-capabilities.md`](https://github.com/OS4CSAPI/ogc-client-CSAPI_2/blob/phase-7/docs/research/requirements/csapi-conformance-capabilities.md)
- [`docs/research/requirements/csapi-gap-analysis.md`](https://github.com/OS4CSAPI/ogc-client-CSAPI_2/blob/phase-7/docs/research/requirements/csapi-gap-analysis.md)
- [`docs/research/requirements/csapi-oshconnect-python-analysis.md`](https://github.com/OS4CSAPI/ogc-client-CSAPI_2/blob/phase-7/docs/research/requirements/csapi-oshconnect-python-analysis.md)
  — the consumer that surfaces in many cs-go issue bodies.
- [`docs/research/requirements/csapi-opensensorhub-analysis.md`](https://github.com/OS4CSAPI/ogc-client-CSAPI_2/blob/phase-7/docs/research/requirements/csapi-opensensorhub-analysis.md)

### Index of external standards

- [`docs/research/references.md`](https://github.com/OS4CSAPI/ogc-client-CSAPI_2/blob/phase-7/docs/research/references.md)
  — annotated bibliography: OGC 23-001, OGC 23-002, SensorML 3.0, SWE Common 3.0,
  related OGC APIs, foundational semantic standards.

### How to use

For each issue evaluation:

1. Identify which CSAPI resource(s) the issue concerns (Datastream? Observation?
   Deployment? query parameters? schema validation?).
2. Open the relevant pre-digested file above to find the right OpenAPI region.
3. **Verify against the bundled OpenAPI directly** — quote the schema verbatim
   in the evaluation record's Evidence block.
4. If a verdict turns on a claim that is not in the OpenAPIs (e.g. SensorML
   semantics, SWE Common encoding details), use `references.md` to find the
   authoritative external standard URL.

**Do not copy these files into the cs-go repo.** They are large and live
canonically in the planning repo; reference by URL and quote the relevant
fragment in the evaluation record.

---

## Evaluation rubric

For each issue we record a verdict on four dimensions, plus an overall
recommendation.

| Dimension | Question | Possible verdicts |
|---|---|---|
| **Validity** | Does the issue describe a real, reproducible problem in the current `main` branch of cs-go? | `confirmed` / `partially-confirmed` / `not-reproducible` / `cannot-determine` |
| **Legitimacy** | Is the described behavior actually wrong relative to the OGC API – Connected Systems specifications and/or stated cs-go design intent? Or is it expected/by-design? | `defect` / `spec-gap-in-cs-go` / `working-as-designed` / `out-of-scope-for-cs-go` / `cannot-determine` |
| **Accuracy** | Are the technical claims in the issue body (file paths, line numbers, function names, error messages, root cause) factually correct? | `accurate` / `mostly-accurate` / `inaccurate-but-symptom-real` / `inaccurate` |
| **Completeness** | Does the issue contain enough information for a downstream contributor to reproduce, scope, and fix without further investigation? | `complete` / `needs-repro-steps` / `needs-spec-citation` / `needs-scope-clarification` / `incomplete` |

### Overall recommendation

One of:

- **Keep / accept as written** — solid issue, ready for triage.
- **Keep with edits** — real issue but body needs corrections (we list them).
- **Reframe** — symptom is real but diagnosis or scope is wrong; rewrite needed.
- **Convert** — should become a different artifact (research note, doc PR,
  upstream-OGC issue, downstream-consumer issue).
- **Close** — not a defect / out of scope / duplicate.
- **Defer pending information** — cannot judge without X.

---

## Methodology (per-issue procedure)

For each issue:

1. **Read the issue body verbatim.** Capture title, labels, body text, comments,
   and linked artifacts. Do not summarize yet.
2. **Identify load-bearing claims.** Enumerate every factual claim the issue
   makes that the verdict depends on (e.g. "function X at file:line does Y",
   "spec section Z requires W", "request R produces response S").
3. **Verify each claim against the source tree** at the cs-go commit current at
   the time of evaluation. Record commit SHA in the evaluation record.
4. **Verify spec claims against the authoritative document.** OGC API – Connected
   Systems Part 1 / Part 2, RFCs cited in those specs, etc. Use direct quotes
   with section references.
5. **Attempt reproduction** when feasible (against `https://129-80-248-53.sslip.io/csapi-go`
   or a local cs-go instance). If not feasible, record why.
6. **Render verdicts** on the four rubric dimensions and an overall
   recommendation. Show the reasoning, not just the verdict.
7. **Record the evaluation** as `docs/research/issue-evaluations/issue-NNN.md`
   following the template below.
8. **Update this plan's tracking table.**
9. **Stop.** Do not proceed to the next issue without user direction.

### Evaluation record template

Each per-issue file contains the sections below. The structure is adapted from
the `phase-6/findings-report-template.md` pattern in the sibling planning repo
and requires explicit Evidence blocks per claim — unsourced reasoning is not
permitted.

```markdown
# Issue #NNN — <title>

- **URL:** https://github.com/OS4CSAPI/connected-systems-go/issues/NNN
- **State at evaluation:** open|closed
- **Labels:** ...
- **Filed by:** <user>
- **Filed:** YYYY-MM-DD
- **Evaluated against cs-go HEAD:** <full-SHA>
- **Date of evaluation:** YYYY-MM-DD

## Sources Consulted

### Primary (authoritative)
- [OGC bundled OpenAPI / cs-go file:line / RFC / etc.] — what was found

### Supporting
- [requirements doc / external link / etc.] — what was used

## 1. Issue body — load-bearing claims

Enumerate every factual claim the verdict depends on, in a table. Do not
paraphrase reasoning; list claims atomically (C1, C2, C3, …) so each can be
verified independently.

## 2. Verification

For each load-bearing claim, apply the per-claim block:

### 2.X — <Claim header>

**Claim (verbatim or close paraphrase):** ...

**Evidence:**

```
<verbatim code, schema fragment, or spec quote with file:line or section ref>
```

**Analysis:** [one paragraph: does the evidence support, refute, or partially
support the claim? what subtlety matters?]

## 3. Reproduction

What was attempted, what succeeded, what was skipped and why. If skipped,
record under §6 Open questions.

## 4. Verdicts

| Dimension | Verdict | Rationale (one sentence) |
|---|---|---|
| Validity | ... | ... |
| Legitimacy | ... | ... |
| Accuracy | ... | ... |
| Completeness | ... | ... |

## 5. Reasoning summary

2–4 paragraphs synthesising the verdicts. May reference §2 evidence by section
number.

## 6. Recommendation

One of the six standard recommendations from the rubric, with concrete edits if
applicable.

## 7. Open questions / unknowns

What the evaluation could not resolve, and what would be needed to resolve it.
```

---

## Issue inventory & tracking

Snapshot taken at start of effort. State and counts will drift; the per-issue
records are the source of truth as evaluations land.

| # | State | Title (verbatim) | Eval status | Verdict | Record |
|---|---|---|---|---|---|
| 1 | open | Datastream creation without explicit `uid` stores empty string, violates unique constraint on second create | complete | Validity: partially-confirmed · Legitimacy: defect · Accuracy: accurate (stale) · Completeness: complete · **Rec: Keep with edits** | [issue-001.md](issue-evaluations/issue-001.md) |
| 2 | open | DELETE on parent resources fails with raw PostgreSQL FK constraint error instead of cascading or returning structured error | complete | Validity: confirmed (symptom) / partially-refuted (causal model) · Legitimacy: defect (compound — cascade impl is itself broken) · Accuracy: accurate-symptom / inaccurate-mechanism · Completeness: incomplete (does not mention `?cascade=true` partial mitigation, does not mention silent orphaning of un-FK'd children, does not mention deployment having no cascade param) · **Rec: Keep with edits, fold cascade-broken finding into body** | [issue-002.md](issue-evaluations/issue-002.md) |
| 3 | open | Research: Time field encoding — strict ISO 8601 string requirement vs. numeric timestamp acceptance | complete | Validity: confirmed (rejection) · Legitimacy: spec-conformant rejection + UX defect + sibling silent-failures · Accuracy: accurate symptom / misleading error message · Completeness: incomplete (omits silent drop on `phenomenonTime` and on Datastream `TimeRange`) · **Rec: Close as research-resolved (no leniency change); F1/F2/F3 filed as #18/#19/#20** | [issue-003.md](issue-evaluations/issue-003.md) |
| 4 | open | Research: NaN handling for numeric observation fields — rejection vs. nilValue support | complete | Validity: confirmed (rejection) · Legitimacy: spec idiom (`"NaN"` string) is the publisher convention; rejecting it is non-conformant; **`nilValues` plumbed-but-unconsumed = functional gap** · Accuracy: accurate symptom; mis-framed as research spike · Completeness: incomplete (missed the `null`-vs-omit asymmetry on `optional:true`, missed the open-schema policy for extra fields) · **Rec: Close as research-resolved with implementation owed; F1/F2/F3 filed as #21/#22/#23** | [issue-004.md](issue-evaluations/issue-004.md) |
| 5 | open | Research: Strict schema validation — requiring ALL declared fields in observation results | complete | Validity: confirmed (rejection on missing required field) · Legitimacy: spec-conformant — `optional: true` mechanism is implemented and works · Accuracy: issue mis-frames cs-go as "always strict" when it is actually "required-by-default with per-field opt-out" · Completeness: incomplete (does not mention the `Optional *bool` schema field that already exists, treats SensorHub leniency as the baseline) · **Rec: Close as research-resolved, no code changes, no new follow-ups (defects in this area already covered by #21/#22/#23)** | [issue-005.md](issue-evaluations/issue-005.md) |
| 6 | open | Research: Cross-resource references — `@link` objects only vs. flat `@id` strings for interoperability | complete | Validity: confirmed (cs-go emits `@link`, not `@id`, on Datastream) · Legitimacy: spec-conformant — OGC 23-002 mandates `@link` for cross-collection refs (Datastream/ControlStream/Deployment/System/SamplingFeature) and `@id` only for in-collection siblings (Observation→datastream, Command→controlstream); SensorHub's `system@id`-on-Datastream is the non-conformant variant · Accuracy: issue's comparative table inverts the conformance polarity · Completeness: incomplete (does not enumerate which fields use which encoding per spec; conflates discoverability with interop fix) · **Rec: Close as research-resolved, no code changes against this issue. Two minor polish items filed as #24 (P3 absolute href) and #25 (P4 populate rel/type/title)** | [issue-006.md](issue-evaluations/issue-006.md) |
| 7 | open | `?uid=` query parameter is silently ignored — server returns all resources unfiltered | complete | Validity: confirmed (`?uid=` is silently ignored on HEAD) · Legitimacy: **NOT a spec-compliance bug** — the OGC CSAPI spec does not define a `uid` query parameter; the spec parameter is `id` and its value may be either local IDs OR URIs (canonical source: `opengeospatial/ogcapi-connected-systems/.../parameters/idList.yaml`). cs-go correctly implements the spec parameter (`id IN ? OR unique_identifier IN ?` across all eight UID-bearing repositories) and `?id=urn:...` returns the matching resource live (T2). The "silent ignore" of `?uid=` is identical behavior to any other unknown parameter (verified `?foo=bar` returns the same unfiltered result, T7). · Accuracy: factual claims about cs-go code are accurate; spec citation ("OGC 23-001 §7.3 defines `uid`") is incorrect · Completeness: incomplete — does not mention that the spec parameter is `id` and accepts URIs · **Rec: Close as research-resolved, NOT a P1 bug. Publisher fix is one character (`?uid=` → `?id=`). Filed #26 (P4 docs) only.** | [issue-007.md](issue-evaluations/issue-007.md) |
| 8 | open | `/deployments` only returns top-level deployments — subdeployments not discoverable without parent ID | complete | Validity: confirmed (T1 numberMatched=1, parent only) · Legitimacy: **spec-conformant defect** — canonical Part 1 OAS for `/deployments` does NOT include the `recursive` parameter and does NOT state any top-level-only default (unlike `/systems` which explicitly does). `paths/subdeployments.yaml` adds an explicit spec note that "individual members can also be retrieved by ID directly at the canonical Deployment resources endpoint", which cs-go violates. · Accuracy: main thrust correct; two specific claims wrong — (a) "consistent with how /systems returns all systems" is false (`/systems` is also top-level-only by default per spec; cs-go matches), (b) "`recursive=true` at top-level is a no-op" is false (T2 returns parent + child). · Completeness: incomplete — missed two additional defects under same root cause: D1 (`?parent=X` at top-level returns 0, T3 — logic conflict with `IS NULL`) and D2 (`?id=<sub-URI>` returns 0, T6 — directly violates the spec note above). All three defects collapse into the same one-block fix. · **Rec: Keep open. Apply Option A (corrected) — remove unconditional `parent_deployment_id IS NULL` clause; closes #8 + fixes D1 + fixes D2. P2 reasonable; defensible P1 because D2 violates an explicit spec sentence. No separate follow-ups filed (single fix). Upstream PR candidate.** | [issue-008.md](issue-evaluations/issue-008.md) |
| 9 | open | Default pagination limit of 10 is unusually low — causes silent result truncation for typical workloads | complete | Validity: confirmed (Limit:10 hardcoded; T1 default GET /systems returns features.Count=10 of numberMatched=13) · Legitimacy: **NOT a defect** — canonical CSAPI parameters/limit.yaml literally specifies `default: 10` and (unlike parent OGC API Features) does NOT include the "values are examples and can be changed" escape clause. cs-go matches the spec exactly. · Accuracy: "unusually low" framing contradicted by issue's OWN comparative table (pygeoapi=10, ldproxy=10, cs-go=10 — only SensorHub=100). · Completeness: misses that #7 was already research-resolved, which the issue body says obsoletes the elevated severity ("becomes cosmetic once #7 is fixed"). · **Rec: Close as wontfix/not-a-defect, OR defer to upstream maintainer UX call. cs-go is spec-conformant. Publisher-side fix (pagination handling) belongs in OSHConnect-Python, not cs-go. No follow-ups filed.** | [issue-009.md](issue-evaluations/issue-009.md) |
| 10 | open | SensorML `documents` array silently dropped — system thumbnails/media links lost | complete | Validity: confirmed (T1 POST `documents` → HTTP 201 → GET back has NO `documents` field; control POST `documentation` → HTTP 201 → GET back HAS the field — definitively isolates bug to JSON struct-tag literal). · Legitimacy: **VALIDATED** — canonical OGC SensorML JSON `DescribedObject.json` literally defines property `documents`; cs-go uses `json:"documentation,omitempty"` in 8 of 9 affected struct fields across 5 files (system.go ×3, deployment.go ×1 + 1 correct, procedure.go ×2, system_event.go ×1, history.go ×1). · Accuracy: issue's spec citation refers to the XML element name (`<sml:documentation>`); JSON encoding renamed to plural `documents` to match `contacts`/`identifiers`/`classifiers`. Likely how the bug was introduced (XML→JSON port). · Completeness: bug scope broader than issue states — affects /deployments, /procedures, /system-events too; all share single fix. · **Rec: P2 firm, defensible P1 (silent data loss with HTTP 201). 9-line search-and-replace fix `documentation` → `documents` plus JSONB column migration. Strong upstream PR candidate.** No follow-ups filed (single root cause covers all 4 resource types). | [issue-010.md](issue-evaluations/issue-010.md) |
| 11 | open | Temporal query parameters (resultTime, phenomenonTime, datetime) silently ignored — all values discarded, including `latest` | complete | Validity: confirmed (9 live probes against `/datastreams/{id}/observations` with seeded 2024/2025/2026 obs all return HTTP 200 and the full unfiltered page, including future-window 2099, garbage `frobnicate`, and the keyword `latest`). · Legitimacy: **VALIDATED** — CSAPI Part 2 §13.3 + OGC API – Features Part 1 §7.15.4 require interval syntax `start/end` (and `..` open-bound forms); CSAPI §13.3.2 statement D normatively requires the `latest` keyword. Features §7.15.4 also requires HTTP 400 for invalid values. · Accuracy: refines issue's "entire parser is no-op" framing. Handler / repository / SQL `WHERE`-builder are correctly wired (lines 109–127 of `observation_repository.go` apply both bounds). The defect is isolated to **one dispatch line**: `ParseTimeRange` routes `[]string` (from `r.URL.Query()[name]`) to `ToTimeRangeFromSlice`, which does not split on `/` and does not handle keywords (`latest`/`now`/`..`/`../..`). The richer `ToTimeRange(string)` path that does handle both is never reached for HTTP query params, and `time.Parse` errors are silently swallowed → `TimeRange{nil,nil}` → no `WHERE` clause → unfiltered HTTP 200. · Completeness: empirical scope limited to observations endpoints; `datetime` on systems/deployments/sampling-features per OGC 23-001r0 §8.7 not probed but plausibly affected by analogous code path. · **Rec: P2-Important firm, defensible P1 (silent HTTP 200 with stale full page; downstream agents polling `?resultTime=latest` get no failure signal). One-function fix in `time_range.go` + one-line change per `*_query_params.go::BuildFromRequest` (route to `ToTimeRange(strings.Join(vals,"/"))` or detect interval/keyword in slice helper). No follow-ups filed (single root cause).** | [issue-011.md](issue-evaluations/issue-011.md) |
| 12 | open | Datastream `unique_identifier` enforces global uniqueness — should be scoped per parent system | complete | Validity: refuted on HEAD — three POSTs of the issue's exact reproducer body across two parent systems (and one within-same-system duplicate) all returned HTTP 201 with distinct ids; the predicted second-POST 500 with unique-constraint violation does not occur. · Legitimacy: **NOT A DEFECT on `c9e4fcf`** — already addressed by commit `1562201` ("update datastreams") which removed `CommonSSN` (and with it the `UniqueIdentifier UniqueID gorm:"uniqueIndex" json:"uid"` field) from `Datastream`, replacing it with explicit `Name`/`Description`. `Datastream` now embeds only `Base` and has no UID field at any level. Reporter's empirical observation was almost certainly accurate against pre-`1562201` code; the issue describes pre-`1562201` schema state. · Spec: canonical CSAPI Part 2 `baseStream.json` ∪ `dataStream.json` schemas define no `uid` property on Datastream — datastreams are identified solely by `id` and are children via `system@link`. The "global vs per-system Datastream UID uniqueness" debate has no spec answer because the property isn't a Datastream property. The 1562201 cleanup (drop entirely — issue's Option B) is spec-faithful; Option A (composite uniqueIndex) would import a System-level concept onto Datastream. · Adjacent findings (out of scope, recommend separate issues): (A) `internal/repository/datastream_repository.go:164` still references the removed `unique_identifier` column in SQL `WHERE id IN ? OR unique_identifier IN ?`, causing every `?id=` filter on `/datastreams` to return HTTP 500 (live-confirmed for garbage, URN, and valid UUID inputs) — collateral damage from the partial 1562201 cleanup, P2; (B) `DELETE /datastreams/{id}` returns HTTP 500 with and without `?cascade=true` — distinct path from finding A — surfaced during cleanup; three test datastreams remain in HEAD DB and require maintainer-side wipe. Note: GORM `AutoMigrate` is additive, so deployments started on `f2cf1c3..1562201^` and never wiped may still carry the legacy column with the original constraint — fresh installs from `c9e4fcf` will not. · **Rec: close as not-a-defect / already-fixed-by-1562201. File findings A and B as separate issues.** Comment posted [#issuecomment-4356771871](https://github.com/OS4CSAPI/connected-systems-go/issues/12#issuecomment-4356771871). | [issue-012.md](issue-evaluations/issue-012.md) |
| 13 | open | POST /systems/{id}/datastreams silently drops stray top-level `uid` field | complete | Validity: confirmed (POST with `uid`, `foo`, nested `properties` all return HTTP 201; GET back contains none of them — silent-drop is reproducible and **uniform across all unknown top-level keys**, not `uid`-specific). · Legitimacy: **NOT A DEFECT** — three independent authorities converge: (a) canonical CSAPI Part 2 `baseStream.json` and `dataStream.json` declare neither `additionalProperties: false` nor `unevaluatedProperties: false`, so per JSON Schema 2020-12 default semantics unknown properties are *permitted by the schema*; (b) RFC 7493 (I-JSON) §4.3 RECOMMENDS ignoring unrecognised members; (c) Go `encoding/json`, OpenAPI 3.x, and mainstream JSON-API convention all default to ignore-unknowns. cs-go's `json.NewDecoder(reader).Decode(&datastream)` is the recommended behaviour; no `DisallowUnknownFields` is needed and adding it would be a breaking deviation. · Accuracy: empirical claim correct; framing as P2-Important data-integrity defect imports an assumption (silent-accept = data loss) not grounded in the spec or RFCs. · Completeness: issue body misses (i) the schema permits unknowns, (ii) silent-drop is generic-not-`uid`-specific, (iii) all three proposed remediation options are spec-deviations or kludges. · Self-correction: this issue was drafted by Copilot during #1 evaluation; rigor-first re-review identifies it as wrong-framing. · Adjacent: cleanup of test datastream `50f019d6-765b-4f51-94b6-037eae06a235` returned HTTP 500 — corroborates #12 finding B; orphan added to the four already pending maintainer wipe. · **Rec: close as wontfix / wrong-framing.** Real concern (publisher migration coordination) belongs in OSHConnect-Python tracker, not as a cs-go server change. | [issue-013.md](issue-evaluations/issue-013.md) |
| 14 | open | GET /api returns 86-byte stub with no paths or schemas (no machine-readable service-desc) | complete | Validity: confirmed empirically — `GET /api` on HEAD (`629e2c4`) returns 88-byte body `{"openapi":"3.0.0","info":{"title":"OGC Connected Systems API","version":"1.0.0"}}` with `Content-Type: application/vnd.oai.openapi+json;version=3.0`; landing page advertises this URL as `service-desc` with matching media type; no `//go:embed` and no static OAS asset anywhere in repo; `internal/api/router.go:255–267` openly carries `// TODO: Implement OpenAPI 3.0 spec generation`. · Legitimacy: **VALIDATED** — three independent normative breaches: (a) **OAS 3.0.3 §4.7.1.1** marks `paths` as **REQUIRED** at the OpenAPI Object root → response is not a valid OpenAPI 3.0 document despite asserting that media type; (b) **OGC API – Common Part 1 (OGC 19-072) `/req/landing-page/api-definition-success` B** — strict `SHALL` "the content of that response SHALL be an API Definition document" — bound to cs-go because `/conformance` declares `…/ogcapi-common-1/1.0/conf/landing-page`; (c) `/req/landing-page/api-definition-success` C — same `SHALL` requires consistency with the negotiated media type, which the body fails. Plus `SHOULD` violation of `/rec/landing-page/api-definition-oas`. · Framing nuance: cs-go does NOT declare `…/ogcapi-common-1/1.0/conf/oas30`, so the stricter `/req/oas30/*` (`oas-definition-2`, `completeness`) requirements are not formally bound — the issue's mention of "Conformance Class OpenAPI 3.0" is therefore slightly imprecise; the actual binding `SHALL` is via `conf/landing-page`. The substance of the defect stands. · Severity: P2-Important is appropriate (machine-discoverability completely broken; clients with hard-coded route knowledge still work; CITE certification would fail). Defensible bump to P1 only if formal OGC certification or generator-driven client tooling is on the roadmap. · Suggested-fix evaluation: all three options (embed static, runtime generation, serve patched upstream OGC OAS31 bundle) are sound; option 3 has lowest authoring delta. · **Rec: KEEP. Comment validates issue + clarifies the binding-`SHALL` framing.** | [issue-014.md](issue-evaluations/issue-014.md) |
| 15 | open | Capital-`S` `Systems` GORM relationship field leaks into every datastream JSON response | complete | Validity: confirmed empirically — `GET /datastreams?limit=1` on HEAD (`e0cb0dc`) returns a top-level `"Systems": null` property on every item; capital-`S`, `null` value, exactly as claimed. · Legitimacy: **VALIDATED, narrowly framed**. Static: `internal/model/domains/datastream.go:57` carries `Systems []System gorm:"many2many:system_datastreams;"` with no `json:"-"` tag, while every other relationship/FK field in the package (10+ instances across `common.go`, `control_stream.go`, `datastream.go`, `deployment.go`, `feature.go`, `procedure.go`, `system.go`, `system_event.go`) carries `json:"-"`. No `MarshalJSON` on `Datastream` itself, so default reflective encoding emits the field. Issue body says "line ~70"; actual is line 57 (trivial drift). · Spec: canonical OGC `dataStream.json` ∪ `baseStream.json` define 21 properties, none named `Systems`. Neither schema declares `additionalProperties: false`, so per JSON Schema 2020-12 default semantics the leak is **not a strict spec violation** — it is a code-quality / API-design / RFC 7493 §4.3 SHOULD-grade interoperability deviation, exactly matching the issue's own P3-Minor + `code-quality`/`api-design` labelling. · Adjacent findings (out of scope per issue's own "file separately" rule): (A) `control_stream.go:53` has the **identical** untagged `Systems []System` field and uses the same direct-`json.Marshal` handler path — currently invisible only because zero controlstreams exist on HEAD; should be a sibling issue. (B) Six latent untagged relationship slices in `system.go:65-70` and `procedure.go:61` (`Procedures`, `Deployments`, `SamplingFeatures`, `Datastreams`, `Controlstreams`, and a third `Systems`) — not visible today because System/Procedure handlers build manual GeoJSON Feature DTOs, but landmines for any future direct-marshal code path; recommend a single broader hardening issue. · Severity P3-Minor firm. Suggested fix is exact and minimal (`json:"-"` appended; no migration, no schema change, no behavioural change; matches convention). · **Rec: KEEP. Comment validates + records adjacent findings A and B for separate filing.** | [issue-015.md](issue-evaluations/issue-015.md) |
| 16 | open | [bug] DELETE cascade is broken across resource types — m2m join FKs not cleaned, and natural parent→child relations have no FK at all | complete | Self-authored umbrella surfaced from #2; this is the rigorous independent re-validation. Validity: all three sub-bugs confirmed on HEAD `edc8459` (no code drift from cited `4b99421`: `git log 4b99421..HEAD -- <repo+handler+domain paths>` returns empty). Bug 1 reproduced live — `DELETE /datastreams/$id?cascade=true` and `DELETE /systems/$id?cascade=true` both 500; non-cascade also 500s when an m2m row exists (every datastream has one because `POST /systems/{id}/datastreams` is the only POST route, per `internal/api/router.go:118-126`). Bug 2 confirmed statically — `DeploymentRepository.Delete(id string)` lacks `cascade bool`, `DeploymentHandler.DeleteDeployment` does not parse `?cascade=`; 3 of 12 repository `Delete` methods support cascade (`System`, `Datastream`, `ControlStream`), 9 do not. Bug 3 confirmed via combined static + DB-introspection evidence — `git grep "foreignKey:" -- internal/model/domains/` returns only **two** active relationship tags (`System.SystemKind`, `System.SamplingFeatures`); the FK list in `docs/research/evidence/issue-002/fk-constraints-head-2026-04-30.txt` shows zero FKs on `observations.datastream_id`, `commands.control_stream_id`, `system_events.system_id`, `system_history_revisions.system_id`, `deployments.parent_deployment_id`. `SystemRepository.deleteCascade` further omits any `SystemEvent` cleanup (zero `SystemEvent`/`system_events` matches in the file), so once Bug 1 is fixed system_events would silently orphan unless added. Currently masked by Bug 1 (m2m FK fires before the natural-side orphaning can manifest); the issue body's "worst possible combination" framing is correct — a Bug-1-only fix would *unmask* Bug 3. · Legitimacy: **VALIDATED in full**. Line numbers, SQL constraint names, structural-asymmetry observations all reproduce exactly. The issue is correctly framed as internal-correctness, not spec-compliance — OGC API CS Part 2 does not normatively mandate cascade vs. restrict semantics. · Severity P1 confirmed (Bug 1 alone makes the canonical teardown workflow unrecoverable through the API). · Recommended remediation reviewed: mirror `SystemRepository`'s `system_deployments`/`system_procedures` cleanup pattern in datastream/controlstream cascades and add `system_datastreams`/`system_controlstreams` cleanup to `deleteCascade`; add `cascade bool` to `DeploymentRepository.Delete`; uniform `mapDeleteError` (23503→409, ErrRecordNotFound→404, default→500) preserving the current SQL-identifier-sanitised body; schema-correct path 3a (proper `gorm:"foreignKey:...;constraint:OnDelete:RESTRICT"` tags) is preferred but should pre-clean any orphaned rows before AutoMigrate adds the constraint, else AutoMigrate would fail. · **Rec: KEEP. Comment validates the umbrella in full and records the AutoMigrate cleanup-pass caveat for the eventual fix PR.** | [issue-016.md](issue-evaluations/issue-016.md) |
| 17 | open | [enhancement] All 12 DELETE handlers map every repository error to HTTP 500 — should distinguish 404 / 409 / 400 / 500 | complete | Self-authored enhancement; rigorous independent re-validation. Validity: central thesis confirmed — `git grep "errors\.As\|pgconn\|SQLSTATE\|StatusConflict" -- internal/api/` returns **zero** matches; FK conflict reproduces as 500 with opaque body. Inventory exact: 12 `r.Delete("/", ...)` routes in `router.go:96-251`, all wired to dedicated handler funcs. · **Two refinements vs. issue body framing**: (1) "no 404 path" overstates — **5 of 12** handlers (Command, Feature, Observation, SystemEvent, SystemHistory) already implement pre-flight `GetByID` 404 at lines 182-186/199-205/155-159/238-241/146-149 respectively; only the other 7 lack it. (2) "everything → 500" mischaracterises the missing-ID case — those 7 no-preflight handlers actually return **204** (not 500) for non-existent IDs because GORM's `db.Delete` returns nil for 0-row matches; 500 is reserved for actual DB-layer errors (FK conflicts being the dominant case). Live-tested: missing-ID-no-preflight → 204, missing-ID-preflight → 404, FK conflict → 500, malformed UUID → **204** (additional gap not in issue body — `/datastreams/not-a-uuid` should 400 but doesn't). · Spec authority: RFC 9110 §15.5.10 (409) citation accurate; §9.3.5 makes 204 for missing-resource DELETE permissible per idempotency, so the 5/7 split is RFC-compliant *in isolation* — the defect is **inconsistency within one API surface**, not non-compliance. No OGC spec involvement. · Severity P2 confirmed (UX/observability, not data correctness). · Remediation review: `classifyRepoError(err) (status, msg)` is structurally correct, with three caveats for the fix PR: (a) `gorm.ErrRecordNotFound` is rarely returned by `db.Delete` so the 404 arm needs `result.RowsAffected == 0` or extension of the pre-flight pattern, (b) malformed UUID should be handled handler-side via `uuid.Parse(id)` → 400 before DB round-trip, (c) consider adding 23514 (check constraint) → 400 arm. · **Rec: KEEP. Comment validates core thesis and records the 5/7 preflight split + 204-on-missing nuance + malformed-UUID gap as public refinements to the status-quo framing.** | [issue-017.md](issue-evaluations/issue-017.md) |
| 18 | open | [bug] TimeRange.UnmarshalJSON silently drops malformed/wrong-typed time values | complete | Self-authored bug; rigorous independent re-validation. Validity: defect confirmed verbatim against current HEAD `08cf161` (no drift vs. cited HEAD `4b994212`). Static — `internal/model/common_shared/time_range.go` `UnmarshalJSON` has **6 silent-discard sites** (4 in array form, 2 in object form) using the pattern `if s, ok := arr[N].(string); ok { if t, err := time.Parse(...); err == nil { tr.X = &t } }`; **plus 2 more** in the string-form fallback `ToTimeRange` (lines 119, 124) using `t, _ := time.Parse(...)` which is *strictly worse* — it discards the parse error AND takes `&t` of the zero-value `time.Time` (`0001-01-01T00:00:00Z`) regardless. Live tests on HEAD `08cf161`: T1 `phenomenonTime:[1773100000.0,null]` (issue body's example) → 201 Created, GET response has no `phenomenonTime` key (silent loss). T2 `["not-a-time","also-bad"]` → 201 Created, identical silent loss. T3 control with valid RFC 3339 strings → 201 + correct round-trip. **Asymmetric strictness** with `Observation.ResultTime` confirmed: `internal/api/observation_handler.go:226-234` has a hand-written decoder that returns `&decodeError{msg:"Invalid resultTime format"}` on parse failure — semantically equivalent input ("a result time on a measurement-bearing resource") is validated on Observation but silently dropped on Datastream. Spec: CSAPI Part-1 §5.1 mandates schema validation on received resources; SWE Common timeInterval encoding requires RFC 3339 strings (or `now`/`..`), JSON numbers are non-conformant; RFC 7493 §3.4 forbids silent type coercion for declared-string fields. Severity P2 confirmed (data integrity — silent acceptance of malformed input materializes records that misrepresent the client's intent; no confidentiality/auth/availability impact). Remediation review: issue body's `fmt.Errorf` per branch is correct as far as it goes but should additionally fix the `t, _ := time.Parse` sites in `ToTimeRange` and consider promoting `ToTimeRange` to return `(TimeRange, error)`. **Rec: KEEP. Comment validates verbatim and records the `ToTimeRange` zero-time-pointer adjacent finding + Observation asymmetry as public refinements.** | [issue-018.md](issue-evaluations/issue-018.md) |
| 19 | open | [bug] decodeObservationPayload silently drops numeric phenomenonTime; should reject symmetrically with resultTime | complete | Self-authored bug; rigorous independent re-validation. Validity: defect confirmed verbatim against HEAD `08cf161` (no drift vs. cited `4b994212`). Static — `internal/api/observation_handler.go:236-243` gates `phenomenonTime` decode on `raw["phenomenonTime"].(string); ok && != ""`; type-assertion failure (e.g. JSON number) silently skips the entire block. Sibling `resultTime` block at lines 226-234 is **structurally identical** (same `raw[...].(string); ok` pattern); the externally-visible asymmetry comes from the late `if obs.ResultTime.IsZero() { return ... "resultTime is required" }` guard at line 272 which is possible only because `ResultTime` is scalar `time.Time` (not `*time.Time` like `PhenomenonTime`). The silent-drop is **symmetric in the decoder**; only the visibility differs. · The masking effect is amplified by `internal/repository/observation_repository.go:22-25`: `if observation.PhenomenonTime == nil { t := observation.ResultTime; observation.PhenomenonTime = &t }` — silently substitutes `resultTime` for the dropped `phenomenonTime`, so GET response shows plausibly-correct `phenomenonTime == resultTime` rather than null. Live tests on HEAD `08cf161`: T1 `phenomenonTime: 1773100000` + valid resultTime → 201, GET returns `phenomenonTime == resultTime` (silent substitution confirmed verbatim). T2 `resultTime: 1773100000` → 400 `"resultTime is required"` (misleading message — field WAS present, late `IsZero()` guard caught it). T3 `phenomenonTime: "not-a-time"` → 400 `"Invalid phenomenonTime format"` (string-form parse path is correctly strict; defect is exclusively in the type-assertion path). · Spec: CSAPI Part-1 §5.1 schema validation requirement; SWE Common JSON encoding (RFC 3339 strings only); RFC 7493 §3.4 (no silent type coercion). · **Adjacent finding** (not in body): same `raw[...].(string); ok` pattern at line 217 for `samplingFeature@id` — JSON number for that field would also silently drop, narrower in practice (no repository default-fill, field documented as string ID everywhere) but same fix shape applies. · Severity P2 confirmed — silent substitution is *worse* than silent drop because GET shows plausibly-correct values, defeating client-side data-loss detection. Remediation review: body's proposed fix shape (`raw["phenomenonTime"]; present && v != nil` with explicit non-string → 400) is correct; should also be applied to `resultTime` (replaces misleading "is required" message for wrong-type case) and optionally `samplingFeature@id`. **Rec: KEEP. Comment validates verbatim and records the symmetric-decoder/asymmetric-visibility precision + repository default-fill amplification + samplingFeature@id adjacent finding.** | [issue-019.md](issue-evaluations/issue-019.md) |
| 20 | open | [enhancement] Misleading error 'resultTime is required' when field is present but wrong type | complete | Self-authored UX enhancement; rigorous independent re-validation. Validity: defect confirmed verbatim against HEAD `08cf161` (no drift vs. cited `4b994212`; verified during #19). Static — `internal/api/observation_handler.go:226-273` gates `resultTime` decode on conjunctive `if resultTimeRaw, ok := raw["resultTime"].(string); ok && resultTimeRaw != ""` (3 conjuncts: present, is-string, non-empty). When ANY fails, block is silently skipped; late guard `if obs.ResultTime.IsZero() { return ... "resultTime is required" }` at line 272 catches the zero-`time.Time` because `ResultTime` is scalar (not `*time.Time`). · **Live matrix wider than body claims**: 7 distinct client error classes tested → only **2 distinct messages** produced. T1 `1773100000.0` (numeric, body's case) → `"resultTime is required"`; T2 `"2026-04-30"` (malformed string) → `"Invalid resultTime format"` (only path with distinct accurate message); T3 missing → `"resultTime is required"`; T4 `null` → same; T5 `""` → same; T6 `{"x":1}` → same; T7 `true` → same. **6 distinct client errors → 1 conflated message** (body identified 2 → 1; the conflation is in fact 4× wider). · Spec: CSAPI doesn't prescribe error wording so no strict spec violation, but defeats RFC 7807 §3 ("identify the problem"); RFC 8259 §3 distinguishes null from missing (body's fix groups them, defensible since client correction is the same). · Body's proposed fix shape (4-branch type-check before parse) correctly disentangles all classes. · **Adjacent finding**: same conflation problem applies in mirror to `phenomenonTime` but inverted (`*time.Time` so no `IsZero()` rescue → silent drop with no error at all — that's #19's data-integrity story). #19 fix and #20 fix fold naturally into a single decoder change. · Severity P3 confirmed (UX/developer-experience, not data correctness, not security). Remediation: low-risk; recommend (a) apply same shape to `phenomenonTime` per body, (b) retain late `IsZero()` as defense-in-depth for non-decoder paths, (c) optionally apply to `samplingFeature@id` per #19's adjacent finding. **Rec: KEEP. Comment validates verbatim and records the wider 6→1 conflation matrix + null-vs-missing nuance + #19 fix-bundling observation.** | [issue-020.md](issue-evaluations/issue-020.md) |
| 21 | open | [P2 bug] Result validator ignores `nilValues` table — spec-idiomatic `"NaN"` / `"-Infinity"` strings rejected as wrong-type | complete | Self-authored P2 bug; rigorous independent re-validation. Validity: defect confirmed verbatim against HEAD `08cf161` (validator + model files byte-identical to cited parent-fork sha `2bbb6202`). Static — `internal/model/domains/datastream.go:235` declares `NilValues []DatastreamNilValue`json tag `nilValues`; struct shape `{Reason string; Value json.RawMessage}` at lines 328-332 — fully plumbed. `git grep -n "NilValues" -- internal/api/` returns **zero matches**; all 7 leaf-scalar branches of `validateDataComponentValue` (`boolean`, `count`, `quantity`, `time`, `category`, `text`, `*range` group) are type-only. Live tests on HEAD `08cf161`: created datastream declaring `nilValues:[{reason:".../missing",value:"NaN"},{reason:".../BelowDetectionRange",value:"-Infinity"}]` on a Quantity; round-trip GET preserves the array verbatim (proves model layer fully functional). T1 `result:{baro_altitude:"NaN"}` → 400 `"must be a number"` (spec expects 201). T2 `"-Infinity"` → 400 same message. T3 valid number 42.5 → 201 (control). T4 `"banana"` (undeclared string) → 400 same message. **T1 and T4 produce identical errors** — proves no `nilValues` consultation. Spec — independently verified against local `docs/research/standards/ogcapi-connectedsystems-2.bundled.oas31.yaml`: the OAS31 declares `nilValues[].value` as `type:string, enum:["NaN","Infinity","+Infinity","-Infinity"]` and ships a working request example using these exact strings — conformance failure is unambiguous. · **Refinement vs body**: defect uniformly affects all 7 leaf-scalar branches (body lists 6; `*Range` branches also need fix). · **Adjacent finding** (body flagged it): `git grep "Constraint\|Updatable" -- internal/api/` returns no validator hits — same model-declared/round-trips/validator-ignores pattern; should be filed as separate issues. · Severity P2 confirmed (functional gap / spec conformance). Remediation: body's `matchesAnyNilValue(component, value)` shape is correct; recommend (a) apply uniformly to all 7 branches, (b) use `bytes.Equal` on `json.RawMessage` for forward compat with hypothetical numeric values, (c) add OAS31-example-verbatim acceptance test. **Rec: KEEP. Comment validates verbatim and records the all-7-branches coverage refinement + identical-error-message proof.** | [issue-021.md](issue-evaluations/issue-021.md) |
| 22 | open | [P3 bug] `null` on `optional:true` numeric field is rejected, while omission is accepted — asymmetric absence | complete | Self-authored P3 bug; rigorous independent re-validation. **Verdict: KEEP with significant reframing — recommend severity downgrade P3-bug → P4-diagnostic.** Behaviour reproduces verbatim against HEAD `08cf161`: T1 omit optional → 201; T2 optional=null → 400 `"must be a number"`; T3 valid number → 201; T4 omit required → 400 `"is required"`; T5 required=null → 400 `"must be a number"`. Static — `internal/api/observation_schema_validation.go:91-95` consults `field.Optional` only inside `if !exists`; JSON null decodes to Go nil interface, key is present but value is nil, optional short-circuit is skipped, recursion hits leaf `quantity` branch and `isNumber(nil)` returns false. Body's code-behaviour description is precise. **However, spec analysis materially changes the verdict**: bundled OAS31 (`docs/research/standards/ogcapi-connectedsystems-2.bundled.oas31.yaml`) line 521 defines `optional` as "data … can be **omitted**" (not "omitted or null"); zero occurrences of `nullable` and zero occurrences of `"null"` JSON-Schema types in the entire OAS31 — Quantity is `type: number`, no null union. Per JSON Schema semantics, `null` is invalid against `type:number`. Current rejection is spec-conformant. SWE Common 3.0's canonical "no value" carriers are omission OR `nilValues` strings (#21); JSON null is not in the SWE Common toolkit. **The body's proposed fix (silently accept null as absence) is NOT RECOMMENDED**: (a) teaches clients a non-canonical encoding, (b) weakens schema enforcement, (c) may obscure real client serializer bugs, (d) creates two competing absence mechanisms in parallel with #21's nilValues addition. Recommended remediation: improve diagnostic message on `value == nil` leaf branches to point clients to omission and nilValues alternatives ("null is not a permitted value; use field omission or a declared nilValue such as \"NaN\""); update user docs to clarify the asymmetry. Adjacent finding (T5): required-field=null produces "must be a number" instead of "is required" — same diagnostic-clarity issue, not a defect since outcome is correct. **Comment will validate behaviour but record the spec-conformance pushback and recommend the diagnostic-improvement path over the body's relaxation proposal.** | [issue-022.md](issue-evaluations/issue-022.md) |
| 23 | open | [P3 docs] Document result-schema validation contract: required/optional, null semantics, `nilValues`, open-schema policy | complete | Self-authored P3 docs issue. **Verdict: KEEP — pure docs deliverable, all 5 claims verified.** Per-claim verification on HEAD `08cf161`: (1) optional ≠ null — cross-ref #22 ✓; (2) nilValues plumbed but not consumed — cross-ref #21 ✓; (3) Open-schema — re-tested live: POST with `undeclared_extra_field:"surprise"` and `another_typo_field:42` on DS-optnull → 201, round-trip GET preserves both verbatim (obsId `d21a2f6a-699d-4e19-8b6c-f47ff157e742`) — confirms typos are indistinguishable from extensions; (4) `isNumber` is `_, ok := v.(float64); return ok` (lines 341-344) — float64-only, works because Go json default decodes numbers to float64; (5) Error format `result.<path> must be a number` — positional dotted path + free-form English, no machine-readable separator. **Repo audit**: `git ls-files docs/api/` returns nothing (dir doesn't exist); only research evidence files match relevant topics — no user-facing docs exist. **Spec**: open-schema is neither mandated nor prohibited (defensible implementation choice); RFC 7807 `application/problem+json` is the OGC-API-conventional error shape and the strongest spec-supported recommendation for the doc. **Refinements to body's deliverable**: (a) doc should reflect #22's reframed outcome (omission canonical for absence; nilValues canonical for "missing-with-reason"; null **not** accepted) — not body's "after #22 lands" framing which assumed null-as-absence; (b) explicit policy decision needed for open-schema (silent-open, open-with-warn-header, or strict); (c) add stability note on numeric type model (UseNumber() switch would silently break isNumber predicates); (d) align error-shape recommendation on RFC 7807 not body's ad-hoc shape. **Adjacent finding**: optional-field typo silently writes the typo and treats the intended field as absent — concrete data-quality risk worth a worked example. Severity P3 correct; depends on #21+#22 landing as filed. **Comment will validate all 5 claims, record live evidence, recommend RFC 7807 alignment, and flag the #22-reframe coordination.** | [issue-023.md](issue-evaluations/issue-023.md) |
| 24 | open | [P3 enhancement] Inline `@link.href` should be absolute URI to match spec `format: uri` | complete | Self-authored P3 enhancement (label: enhancement). **Verdict: KEEP**, P3 confirmed. Symptom reproduces verbatim on HEAD `08cf161`: `GET /datastreams?limit=1` returns `system@link.href = "systems/666ed3fe-bccf-4670-bb85-a0a3c4562772"` (relative) while the same response's `links[]` entry with `rel:ogc-rel:systems` is fully absolute `https://129-80-248-53.sslip.io/csapi-go-head/systems/666ed3fe-...` — internal inconsistency confirmed within a single response. Static — root cause is at server-synthesis sites: `internal/api/datastream_handler.go:136` and `internal/api/control_stream_handler.go:145` build `&Link{Href: "systems/" + systemID}` without going through `formaters.ToFunctionalAssociationHref(...)`. Helper at `association_links.go:371-389` is idempotent on already-absolute inputs (`parsed.IsAbs()` short-circuit) and is configured with `cfg.API.BaseURL` at startup (`router.go:27`) — same helper is producing the absolute `links[]` array entries. **Round-trip test (T2)**: POST with `system@link:{href:"https://example.org/api/systems/<uuid>"}` → 201 → GET preserves absolute href verbatim → proves fix can be applied with no breakage to compliant clients (dsId `c8a85e2c-3e99-4708-8d95-9ffaa28b72ed`). Spec — body's citation verified independently against `docs/research/standards/ogcapi-connectedsystems-2.bundled.oas31.yaml`: inline `@link` schema (line 312-324) defines OGC Link object with `href: format: uri` (reused via `*ref_11`/`*ref_12` for all 17 inline link properties); worked examples at lines 1922-1929 (DS) and 3547-3550 (Command) uniformly use absolute `https://...` URIs; spec explicitly uses `format: uri-reference` elsewhere (line 549) for XLink-style associations — the choice of `uri` here is intentional. **Spec-strictness nuance**: `format: uri` in JSON Schema 2020-12 / OpenAPI 3.1 is annotation-only by default; strict validators using format-assertion vocabulary flag relative form. Not a hard interop break (RFC 3986 §5 base-resolution works) but a real format-vocabulary lint + internal-consistency violation + spec-example mismatch. **Refinements to body's fix sketch**: (a) recommend **both** handler-synthesis sites AND formatter sites (defence in depth, helper is idempotent so safe), (b) acceptance criterion should assert `^https?://` for every inline `@link.href` across all 7 resource types, not only DS/CS — affected fields enumeration in body is exact (17 properties total). **Comment posted: validates fully, sharpens fix scope to handler + formatter, calibrates spec-strictness expectation.** | [issue-024.md](issue-evaluations/issue-024.md) |

`Eval status` values: `not-started` / `in-progress` / `complete`.

---

## Process notes

- **Order.** Evaluations proceed in numeric issue order by default. The user may
  reorder at any time; the tracking table reflects the actual order taken.
- **Branch.** All evaluation work lives on the `issues-evaluation` branch
  (created off `main`). Each evaluation record lands as a separate commit so the
  history of judgments is auditable.
- **No code changes.** This effort produces only documentation. Source code is
  not modified by this plan. Any code-change recommendations that emerge are
  recorded as recommendations only.
- **Plan amendments.** This file may be amended as we learn (e.g. discovering
  that a dimension is too coarse, or that the rubric needs another verdict
  value). Amendments are committed separately from per-issue work and noted in a
  changelog at the bottom of this file.

---

## Changelog

- **2026-04-30** — Plan created. 12 issues open at start (#1–#12). No
  evaluations begun.
- **2026-04-30** — Issue #1 evaluated. Verdict: keep with edits. Surfaced a
  partial-fix-without-closure pattern in commit `1562201` and a compile break in
  `generators_datastream.go`. Cross-referenced #12 as sibling.
- **2026-04-30** — Plan amended: added "Authoritative resources" section
  pointing at the bundled OGC OpenAPIs and pre-digested requirements docs in the
  sibling planning repo (`OS4CSAPI/ogc-client-CSAPI_2:phase-7/docs/research`).
  Replaced flat per-issue template with a per-claim Evidence-block template
  adapted from `phase-6/findings-report-template.md`. Added mandatory
  "Sources Consulted" header section. Issue #1 record updated retroactively to
  match new template and to resolve the OGC 23-002 §9.2 spec question using the
  bundled Part 2 OpenAPI.
- **2026-04-30** — Issue #1 record strengthened: cross-fork verification confirms compile break is in the maintainer's canonical `SomethingCreativeStudios/connected-systems-go` HEAD too (not an OS4CSAPI-fork artefact). Live deployment probe at `https://129-80-248-53.sslip.io/csapi-go/` shows the deployed binary is pre-`1562201` (response includes populated `uid` field that current struct cannot produce). references.md cross-checked: only Part 1 + Part 2 bundled OAS31 are authoritative for CSAPI resource schemas; SensorML/SWE Common JSON Schemas govern other layers.
- **2026-04-30** — Issue #1 record corrected: scope of the build break narrowed. `go build ./cmd/server` succeeds (EXIT=0); only `go build ./...` and `go test ./...` fail because `internal/model/generators/` is a test-fixtures helper that still references the removed `CommonSSN`. Earlier framing of "the project doesn't build" was an overstatement. **Methodology note for future evaluations:** when reporting a build failure, always run the deployable-artefact build separately from the module-wide build before claiming "the project is broken" — they are different signals. Test-only breaks are still defects but should be characterised as such.
- **2026-04-30** — Parallel HEAD deployment brought up at `https://129-80-248-53.sslip.io/csapi-go-head/` (cs-go `4b99421`, fresh DB on port 8283 alongside the existing pre-`1562201` stack on 8282). Project name `csapi-head`, separate Postgres volume `postgres_data_head`, dedicated Caddy route `/csapi-go-head/*`. The Dockerfile builds only `cmd/server/main.go` so the §2.5 test-fixtures break is not a build blocker for the deployed image. This deployment is a clean A/B testbed for the rest of the issue evaluations and answers several open questions on issue #1 directly.
- **2026-04-30** — Issue #1 record extended with empirical live evidence (§2.6 update + new §2.8): three POST variants against `/systems/{id}/datastreams` on HEAD (no-uid, repeat, stray-uid) all return `201 Created` with no `uid` in responses; HEAD-initialized DB has no `unique_identifier` column or unique index; production DB still has both. Open questions §7.2 (live DB schemas) resolved. Three new follow-up surfaces noted (§7.6 stray-uid silent-drop, §7.7 `/api` is a stub, §7.8 capital-S `Systems` field leaks into JSON) — these will be filed as separate issues if they aren't already tracked. Raw transcripts saved under `docs/research/evidence/issue-001/`.
- **2026-04-30** — Issue #2 evaluated. Verdict: keep with edits. Symptom (DELETE → 500 on linked resources, blocks teardown) is fully reproducible on HEAD and prod. Causal model in issue body is partially wrong: (a) blocking FKs are on GORM-generated m2m **join tables** (`fk_system_datastreams_*`, `fk_system_controlstreams_*`), NOT on natural parent→child columns — `observations.datastream_id` and `commands.control_stream_id` have **no FK at all**, so DELETEs of un-linked resources silently orphan their children; (b) response body is sanitized (`{"error":"Failed to delete X"}`, 40B), the raw PG error is in server logs only — refutes issue's claim of raw-error exposure; (c) `?cascade=true` IS exposed on Datastream/ControlStream/System handlers but is **empirically broken** (T8/T11 both 500 — cascade impl deletes children but does not clear the m2m join, then the resource delete trips the same FK); (d) `DeploymentRepository.Delete` has no cascade parameter at all. Evaluation results posted as [comment on #2](https://github.com/OS4CSAPI/connected-systems-go/issues/2#issuecomment-4355934881). Two follow-up issues filed via `mcp_io_github_git_issue_write`: **#16** ([bug] DELETE cascade is broken across resource types) bundles cascade-impl breakage + missing cascade param on Deployment + silent-orphaning hazard into one umbrella for coordinated fix; **#17** ([enhancement] DELETE handlers map every error to 500) is the orthogonal contract fix (23503 → 409, ErrRecordNotFound → 404). **Methodology note:** PowerShell SSH heredoc nested-quoting is a footgun for `psql -c "… contype='f' …"` — escaped single-quotes get stripped silently and yield zero rows that look like "no FKs exist". Use `chr(102)` or shell-escape carefully; cross-check the result against actual server-log error messages before drawing structural conclusions. **Process note:** all evaluation work gets posted upstream — comment on the evaluated issue + new follow-up issues + local docs commit, not just local docs. Raw transcripts saved under `docs/research/evidence/issue-002/`.
- **2026-04-30** — Issue #3 evaluated. Verdict: research-resolved (no leniency change recommended). The original conformance question has a clear spec answer: OGC 23-002 OAS31 defines `phenomenonTime`/`resultTime` as `type: string, format: date-time`, so cs-go's rejection of numeric epoch is correct per spec; SensorHub's leniency is a non-conformant convenience. The *productive* findings came from live A/B testing on HEAD: (a) the rejection error message `"resultTime is required"` is **misleading** — the field IS present, just wrong type — this is the actual UX defect that prompted the spike (filed as **#20**, P3 UX); (b) numeric `phenomenonTime` is **silently dropped** with HTTP 201 — asymmetric vs. `resultTime`'s reject (filed as **#19**, P2 data-integrity); (c) `TimeRange.UnmarshalJSON` (used by Datastream `phenomenonTime`/`resultTime`) **silently swallows** non-string array elements AND silent-drops parse failures, so Datastream POST with numeric or malformed time arrays returns 201 with empty TimeRange (filed as **#18**, P2 data-integrity). Evaluation posted as [comment on #3](https://github.com/OS4CSAPI/connected-systems-go/issues/3#issuecomment-4356023583). Raw transcripts + spec extract under `docs/research/evidence/issue-003/`. **Methodology note:** when a research spike asks "should we accept X?", run live tests for both X-rejected paths AND look for sibling fields where the same wrong-type input leaks through silently — asymmetric strictness across siblings is usually a worse defect than either pure-strict or pure-lenient.
- **2026-04-30** — Issue #4 evaluated. Verdict: research-resolved with implementation owed. The spike's core question ("should we accept `NaN` / `null` / sentinel?") has a clear spec answer — SWE Common's per-field `nilValues: [(reason, value), …]` table IS the canonical mechanism, and the OGC OAS31 examples explicitly use `value: NaN` / `'-Infinity'` / `+Infinity` for Quantity components. The publisher's `"NaN"` string is therefore the spec idiom (RFC 8259 forbids bare `NaN`/`Infinity` literals). **Headline finding:** `DatastreamDataComponent.NilValues` is plumbed through the data model and round-trips on GET, but the result validator (`validateDataComponentValue` in `internal/api/observation_schema_validation.go`) never reads it — `grep -rn NilValues internal/api/` returns zero hits. Same defect ships in the parent fork (sha `2bbb6202`). 16 live test cases on HEAD confirm: T1/T2 — `"NaN"` rejected identically whether nilValues is declared or not; T6 — numeric `-999.0` accepted as a Quantity by type coincidence regardless of nilValues; T11 — extra undeclared fields silently accepted and persisted (open-schema policy, undocumented); T14/T15 — `optional:true` covers omission but NOT explicit `null` (asymmetric absence, same pattern as #19). Evaluation posted as [comment on #4](https://github.com/OS4CSAPI/connected-systems-go/issues/4#issuecomment-4356154026). Three follow-ups filed: **#21** ([P2 bug] result validator ignores `nilValues` table — the headline functional gap), **#22** ([P3 bug] `null` on `optional:true` numeric rejected while omission accepted — sibling of #19), **#23** ([P3 docs] document the result-schema validation contract: required/optional, null semantics, nilValues, open-schema policy for extra fields). Raw transcripts + spec extract + static-analysis snapshot under `docs/research/evidence/issue-004/`. **Methodology note:** "plumbed-but-unconsumed schema fields" is a sibling pattern to "type-assert-and-discard" — fields like `NilValues`, `Constraint`, `Updatable` survive the JSON round-trip but no code reads them in the request path. Future audits should grep `internal/api/` for consumers of every declared schema field, not just inspect the model definitions.
- **2026-04-30** — Issue #5 evaluated. Verdict: research-resolved, no code changes, no new follow-ups. The issue's premise ("server strictly requires ALL declared fields") is **mistaken**: cs-go is required-by-default with per-field `optional: true` opt-out, exactly per SWE Common 3.0 §7.4. `DatastreamDataComponent.Optional *bool` is in the model (line 225) and is consumed correctly in the `datarecord` (line 92) and `vector` (line 115) branches of `validateDataComponentValue`. T1/T2 prove the optional mechanism works end-to-end: identical 6-field schema, same payload omitting `timestamp` — DS_ALLREQ rejects (400), DS_TS_OPT (with `timestamp: optional:true`) accepts (201). The OSHConnect-Python publisher's `duplicate-resultTime-into-result.timestamp` workaround was unnecessary; declaring `timestamp` as optional in the datastream schema would have worked. T6 (typo case) confirms the open-schema sibling defect already filed under #23: a typo'd field name is silently accepted as an extra while the declared field reports missing. Evaluation posted as [comment on #5](https://github.com/OS4CSAPI/connected-systems-go/issues/5#issuecomment-4356212161). Defects in this area already tracked under #21/#22/#23. **Methodology note:** when an issue claims "the server is too strict", first verify whether the spec mechanism for opt-out is implemented and whether the publisher used it. Sometimes the right answer is publisher-side discoverability, not server leniency. Deferred audit: `datachoice`/`dataarray`/`matrix` branches do not consume `optional` on subcomponents; not pursued here because no observed publisher hits this.
- **2026-04-30** -- Issue #6 evaluated. Verdict: research-resolved, no code changes against this issue. **Headline:** the issue's comparative table inverts the conformance polarity. OGC 23-002 mandates `@link` (object) for cross-collection refs (Datastream/ControlStream system/procedure/deployment/featureOfInterest/samplingFeature; Deployment platform/deployedSystems; SamplingFeature sampledFeature; System systemKind; Observation procedure/result; Command procedure) and `@id` (flat string) only for in-collection siblings (Observation.datastream@id REQUIRED, Command.controlstream@id REQUIRED, CommandStatus.command@id REQUIRED, plus same-collection samplingFeature@id on obs/cmd). There is **no `system@id` field defined on Datastream or ControlStream** anywhere in the spec — SensorHub's emission of it is the non-conformant variant. cs-go's model (`internal/model/domains/*.go`) and serialization (`json_formatters/datastream_json.go`) match the spec field-for-field and emit the conformant shape end-to-end (verified live on HEAD: T1/T3 transcripts in `docs/research/evidence/issue-006/`). Recommendation against emitting both `system@id` AND `system@link` on Datastream — would be a non-spec extension creating two sources of truth. Client-side fix is correct (already done in ogc-client-CSAPI_2 #166). Evaluation posted as [comment on #6](https://github.com/OS4CSAPI/connected-systems-go/issues/6#issuecomment-4356255462). Two minor polish items filed: **#24** ([P3 enhancement] inline `@link.href` should be absolute URI to match spec `format: uri` and the existing `links[]` convention), **#25** ([P4 enhancement] populate `rel`/`type`/`title`/`uid` on inline `@link` objects — pure UX win, zero spec risk). **Methodology note:** this was the inverse of #4. For #4, model fields were plumbed but not consumed; for #6, every field IS consumed and IS conformant — the issue's premise was a client expectation calibrated on a different (non-conformant) server, projected onto the conformant one as a 'missing feature'. Future spikes that ask 'why doesn't server X look like server Y?' should start with the spec inventory, not the comparative table; if the spec defines exactly one shape, the asymmetry is a defect on the side that lacks the conformant shape, not on the side that has it.
- **2026-04-30** -- Issue #7 evaluated. Verdict: research-resolved, **NOT** a P1 spec-compliance bug as filed. **Headline:** the OGC CSAPI spec does not define a `uid` query parameter. Direct inspection of the canonical opengeospatial source (`api/part1/openapi/parameters/idList.yaml`) shows the parameter is named `id` with description 'List of resource local IDs or unique IDs (URI)' and explicit examples for both Local IDs and URIs. The same shape is referenced from every list endpoint via `  id`. cs-go's repositories correctly implement the spec dual-match: `query.Where("id IN ? OR unique_identifier IN ?", params.IDs, params.IDs)` across system/datastream/control_stream/deployment/procedure/property/sampling_feature/feature repos. Live verification on HEAD: T2 `GET /systems?id=urn:test:issue1:sys:1` returns numberMatched=1 (the spec URI branch works); T3 `GET /systems?uid=urn:...` returns numberMatched=12 (silently ignored — same as T7 `?foo=bar` which also returns 12, confirming the mechanism is generic 'unknown parameter' fall-through, not a missing implementation). The publisher's reported workaround (fetch `?limit=1000` + client-side scan) is unnecessary — replacing `?uid=urn:...` with `?id=urn:...` works on cs-go today and on any spec-compliant server. The issue's citation 'OGC 23-001 §7.3 defines uid as a standard query parameter' is incorrect — that paragraph defines `id`. Severity downgraded from P1-Critical to non-defect. Evaluation posted as [comment on #7](https://github.com/OS4CSAPI/connected-systems-go/issues/7#issuecomment-4356304779). One follow-up filed: **#26** ([P4 docs] document that `?id=` accepts both local IDs and URIs — the spec UID filter is `?id=`, not `?uid=`); explicitly NOT filing the `uid`→`id` alias as a follow-up because it would be a non-spec extension and the publisher fix is one character. **Methodology note:** this is the third issue in a row (#5 -> #6 -> #7) where comparative-server framing inverted the conformance polarity — SensorHub's behavior was treated as the baseline and cs-go's deviation from SensorHub was read as a defect; in all three cases direct spec inspection showed cs-go matches the spec and SensorHub deviates. The decisive evidence here was a single fetch of `parameters/idList.yaml` from opengeospatial. Rule going forward: when an issue says 'server X is missing feature Y that server Z has', open the canonical OGC source for the feature **by name** before stopping at the comparative table.
- **2026-04-30** -- Issue #8 evaluated. Verdict: **VALIDATED with corrections.** First issue in the run where the spec sided with the report on its main thrust. Spec evidence (canonical Part 1 OAS): /systems explicitly says "By default, only top level systems are included" and includes the 
ecursive parameter; /deployments says "List or search **all** Deployment resources" and does **not** include 
ecursive. paths/subdeployments.yaml adds the explicit guarantee "individual members can also be retrieved by ID directly at the canonical Deployment resources endpoint." cs-go's unconditional WHERE parent_deployment_id IS NULL on /deployments default is a non-spec restriction. Live verification on HEAD (created urn:test:issue8:dep:parent + urn:test:issue8:dep:child via canonical + subdeployments POST endpoints): T1 GET /deployments=1 (parent only), T2 GET /deployments?recursive=true=2 (parent+child) **contradicting the issue's no-op claim**, T3 GET /deployments?parent={pId}=0 **NEW defect D1** (logic conflict — parent filter AND'd with IS NULL → impossible), T4 /{pId}/subdeployments=1 ok, T5 /deployments/{cId} direct=found ok, T6 GET /deployments?id=urn:test:issue8:dep:child=0 **NEW defect D2** (canonical URI lookup of subdeployment fails — directly violates spec note). Two specific claims in the issue body are factually wrong: (a) "/systems returns all systems" — false, system_repository.go:386-389 also defaults top-level only via if !params.Recursive; (b) "
ecursive=true is a no-op" — false, T2 returns 2. Recommendation: keep open, apply Option A (refined with spec citation in comment) — single block change closes #8 + D1 + D2 simultaneously. P2 reasonable; defensible P1 case via D2 (explicit spec sentence violated). No separate follow-ups filed because all three defects share one fix. Strong upstream PR candidate. Methodology rules added: (1) never accept a comparative-server claim without fetching the canonical OAS for both endpoints by name; (2) when a code branch is described as a no-op via comment, run it before believing the comment. Comment posted at #8: https://github.com/OS4CSAPI/connected-systems-go/issues/8#issuecomment-4356426711
- **2026-04-30** -- Issue #9 evaluated. Verdict: **NOT a defect.** cs-go matches the canonical CSAPI spec default (parameters/limit.yaml: default: 10) literally. Unlike the parent OGC API Features spec, CSAPI's limit.yaml does NOT include the "values are examples and can be changed" escape clause — the value is stated literally. Issue's own comparative table refutes its own framing: pygeoapi=10, ldproxy=10, cs-go=10 — only SensorHub=100 deviates. "Unusually low" concentrates entirely on SensorHub-as-baseline (same pattern as #5/#6/#7). Live verification on HEAD: T1 default GET /systems returns features.Count=10 (numberMatched=13); T2 ?limit=5 returns features.Count=5; baseline ?limit=1000 returns features.Count=13. Static claim accurate; spec premise inverted. The issue's own self-downgrade ("becomes cosmetic once #7 is fixed") landed right — #7 was research-resolved, so the publisher's ?limit=1000 workaround is unnecessary because the spec parameter id accepts URIs and one-character ?uid= -> ?id= removes the find_by_uid pagination dependency entirely. Recommendation: close as wontfix/not-a-defect or defer to upstream maintainer UX call. Raising default to 100 would deviate FROM the literal CSAPI spec but TOWARD the parent OGC API Features value — a deployer UX choice, not a spec fix. No follow-up issues filed against cs-go because there is nothing to fix; pagination-handling fix belongs in OSHConnect-Python. Methodology rules confirmed: (1) Even an issue's own comparative table can refute its own framing — cross-check evidence against framing; (2) Self-downgrade clauses are often correct, check dependency-resolution status early; (3) SensorHub-as-baseline pattern now confirmed in 4 of 9 issues evaluated. Comment posted: https://github.com/OS4CSAPI/connected-systems-go/issues/9#issuecomment-4356593770
- **2026-04-30** -- Issue #10 evaluated. Verdict: **VALIDATED. Real defect, well-isolated.** Recommend P2 firm with defensible P1 case (silent data loss with HTTP 201). Root cause: cs-go's JSON struct tag is json:"documentation,omitempty" but the canonical OGC SensorML JSON encoding DescribedObject.json literally defines the property as documents. Go's default ncoding/json silently drops unknown fields on unmarshal, so spec-correct documents arrays from clients are discarded without any signal. HTTP 201 misleads clients into thinking the payload was accepted intact. Live verification on HEAD with definitive isolation: T1 POST with documents array (spec-correct) returns 201 but GET back contains NO documents field; control POST with documentation (cs-go's wrong tag) returns 201 and GET back FULLY preserves the array. Underlying types, JSONB persistence, and marshal/unmarshal pipeline all work — bug is isolated entirely to the json tag literal. Static inventory: 9 affected struct fields across 5 files (system.go ×3, deployment.go ×1+1 correct, procedure.go ×2, system_event.go ×1, history.go ×1); 8 of 9 use the wrong tag. Internal inconsistency: deployment.go:34 GORM struct correctly uses documents but the deployment SML output struct on line 112 still says documentation, so /deployments wire format is broken even though the DB column name is correct. Scope broader than issue states — affects all 4 SensorML-accepting resource types (/systems live confirmed; /deployments, /procedures, /system-events confirmed by static inspection). Issue's own spec citation ("SensorML 2.1 AbstractProcess → documentation/DocumentList") refers to the XML element name <sml:documentation> containing a <DocumentList> — when the SensorML SWG ported to JSON encoding they renamed the property to plural documents to match contacts/identifiers/classifiers. The cs-go tag spelling fits the profile of a naive XML→JSON port that kept the XML element name. Recommended fix: 9-line search-and-replace documentation → documents plus regression test plus JSONB column migration. No follow-ups filed because all 4 resource types share a single root cause and single fix. Strong upstream PR candidate (SomethingCreativeStudios/connected-systems-go). Methodology rules added: (1) Spec-name vs encoding-name vs struct-name vs column-name are four separate concepts — always verify the encoding-specific name from the canonical encoding schema, not prose, not XML element names, not sibling fields. (2) Issue's own spec citation can be a misleading translation — re-derive the encoding name independently. (3) Internal inconsistency (correct tag in 1 place, wrong tag in 8) is a smell of half-awareness — signals partial prior awareness without complete propagation. (4) Control tests with the wrong-but-matching name cheaply isolate root cause for any "field silently dropped" claim. Comment posted: https://github.com/OS4CSAPI/connected-systems-go/issues/10#issuecomment-4356650474
- **2026-04-30** -- Issue #11 evaluated. Verdict: **VALIDATED. Real defect, well-isolated.** Refines issue framing: the temporal parser is NOT entirely a no-op — handler / repository / SQL `WHERE`-builder are all correctly wired (observation_repository.go::applyFilters lines 109-127 add WHERE result_time / WHERE phenomenon_time clauses when bounds are non-nil). The defect is one dispatch line in time_range.go::ParseTimeRange: the `case []string` branch routes HTTP query params to ToTimeRangeFromSlice, which calls time.Parse(RFC3339, parts[0]) directly without splitting on `/` and without recognising the spec keywords (latest/now/.. /../..). The richer ToTimeRange(string) function in the same file does both, but is never reached for HTTP query params (the handler always passes vals=[]string from r.URL.Query()[name]). time.Parse errors are silently swallowed, so every value of resultTime/phenomenonTime — interval syntax, garbage, keywords — yields TimeRange{nil,nil} → no WHERE clause → unfiltered HTTP 200 default page. Live verification on HEAD: seeded 3 observations on cf4cd80c (resultTime in 2024/2025/2026); 9 probes (latest, latest&limit=1, future-2099-window, garbage `frobnicate`, narrow valid result-window, phenomenonTime=now, narrow valid phenomenon-window, open-start `../end`, open-end `start/..`) all return HTTP 200 with all 3 observations. Only latest&limit=1 returns 1 item, by accident of unfiltered ORDER BY result_time DESC LIMIT 1. Spec authority: CSAPI Part 2 §13.3 (defines resultTime/phenomenonTime), OGC API Features Part 1 §7.15.4 (interval grammar; HTTP 400 required for invalid input), CSAPI Part 2 §13.3.2 statement D (latest keyword normative). Severity: P2-Important firm; defensible P1 because failure is silent (HTTP 200 with stale full page; agents polling ?resultTime=latest get no failure signal). Suggested fix: change BuildFromRequest to call ToTimeRange(strings.Join(vals,"/")), or extend ToTimeRangeFromSlice to detect interval-with-slash and keywords and delegate; bubble parse errors so handler can return HTTP 400 on garbage. Methodology rules confirmed: (1) when static analysis predicts a code path runs but live test contradicts it, suspect a parse-side dispatch issue rather than missing wiring; (2) seed-and-probe with widely-separated timestamps (multi-year apart) makes filter-vs-no-filter trivially distinguishable; (3) issue's empirical claim can be true even when its mechanistic framing is too strong — refine, don't reject.
  Comment posted: https://github.com/OS4CSAPI/connected-systems-go/issues/11#issuecomment-4356712909
